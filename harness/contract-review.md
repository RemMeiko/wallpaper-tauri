# 合同审查: Sprint 3

## 评定: 通过

## 范围覆盖

合同范围与 spec 中 Sprint 3 定义高度一致。spec 要求三个交付物：

1. **"设置" Tab 完整功能界面** — 合同第 2 节（设置页 UI）覆盖，包含下载目录、本地壁纸目录、轮换开关、轮换间隔、轮换模式，布局使用 Card 分组，符合 spec 要求。
2. **配置持久化到本地文件** — 合同第 1 节（配置持久化）覆盖，存储位置 `~/.wallpaper_app/settings.json` 与 spec 一致，`load_settings` / `save_settings` 命令对齐 spec 验收标准。
3. **壁纸自动轮换定时器** — 合同第 3 节（壁纸自动轮换）覆盖，包含 start/stop/status 命令、随机/顺序模式、生命周期管理。

合同额外包含了第 4 节（前端集成）和第 5 节（遗留问题处理），前者是必要的胶水工作，后者是对前序 Sprint 遗留问题的主动修复，均合理。

**无遗漏，无超出范围。**

## 验收标准审查

### 配置持久化（标准 1-4）
- 标准 1（load_settings / save_settings 命令 + 存储位置）: 充分，可通过检查代码和实际文件验证。
- 标准 2（默认值回退）: 充分，列出了具体默认值（下载目录、图片目录、轮换关闭、30 分钟、随机），可精确验证。
- 标准 3（启动时自动加载）: 充分，可通过检查 App.tsx 初始化逻辑验证。
- 标准 4（修改后保存 + 重启恢复）: 充分，端到端可测试。

### 设置页 UI（标准 5-8）
- 标准 5（完整配置界面）: 充分，列出了所有配置项。
- 标准 6（目录选择交互）: 充分，Input + 文件夹按钮模式明确。
- 标准 7（轮换控件）: 充分，Toggle / 数字输入框（1-1440）/ Select 下拉框，控件类型和范围均明确。
- 标准 8（自动保存 + toast 提示）: 充分，可观察验证。

### 壁纸自动轮换（标准 9-13）
- 标准 9（定时器启动 + 自动切换）: 充分。
- 标准 10（随机模式选择不同壁纸）: 基本充分，但"不同"的定义略模糊 — 随机选择不保证连续两次不重复。考虑到合同"不做的事"中明确排除了轮换历史记录，这个程度可以接受。
- 标准 11（顺序模式按文件名顺序）: 充分，可通过检查排序逻辑验证。
- 标准 12（关闭后停止）: 充分。
- 标准 13（前端显示运行状态）: 充分，合同实现计划中明确了 `RotationStatus` 结构包含 `running` 和 `next_change_at`。

### 遗留问题修复（标准 14-17）
- 标准 14（设为壁纸按钮单选约束）: 充分，直接对应 Sprint 2 遗留 #2。
- 标准 15（动画延迟上限 300ms）: 充分，`Math.min(index * 30, 300)` 方案明确，同时修复 LocalWallpaperTab 和 WallpaperGrid，对应 Sprint 2 遗留 #3 + Sprint 1 遗留 #3。
- 标准 16（useMemo 缓存 canSetWallpaper）: 充分，对应 Sprint 2 遗留 #4。
- 标准 17（doLoadMore resultText 闭包问题）: 充分，函数式 setResults 方案明确，对应 Sprint 1 遗留 #1。

### 构建验证（标准 18-19）
- 标准 18（前端构建通过）: 充分。
- 标准 19（Rust release 编译通过）: 充分。

## 遗留问题处理评估

合同第 5 节对 `evaluation.md` 中 6 项遗留问题的处理：

| 遗留问题 | 来源 | 合同处理 | 评估 |
|----------|------|----------|------|
| doLoadMore resultText 闭包捕获 | Sprint 1 #1 | 修复（标准 17） | 合理 |
| sorting=random 跨页重复 | Sprint 1 #2 | 不修复（API 固有行为） | 合理，接受为已知限制 |
| 动画延迟累积 | Sprint 1 #3 + Sprint 2 #3 | 修复（标准 15） | 合理，两处同源问题一并修复 |
| 合同与实际修复方案不一致 | Sprint 2 #1 | 不修复（文档问题，handoff 补充说明） | 合理 |
| 搜索页设为壁纸多选行为不明确 | Sprint 2 #2 | 修复（标准 14） | 合理 |
| canSetWallpaper 每次渲染创建新数组 | Sprint 2 #4 | 修复（标准 16） | 合理 |

6 项遗留问题中修复 4 项、合理不修复 2 项，处理方式均有明确理由。作为最后一个 Sprint，这个处理是负责任的 — 不修复的 2 项均有充分理由（API 固有行为、文档问题不影响功能）。

## 修改建议

无强制修改要求。以下为可选优化建议（不阻塞通过）：

1. **标准 10 措辞微调**: "选择不同壁纸"建议改为"随机选择壁纸"，避免暗示去重保证。当前"不做的事"已排除轮换历史，措辞对齐更清晰。
2. **配置文件损坏恢复策略**: 合同风险部分提到"JSON 解析失败时回退到默认值并覆盖写入修复"，建议在覆盖前 log 一条警告，方便用户排查。这不需要加为验收标准，实现时注意即可。

## 测试计划预览

评审阶段我将重点验证以下方面：

### 配置持久化
1. 检查 Rust 后端 `load_settings` / `save_settings` 实现，确认默认值逻辑、文件读写、错误处理（文件不存在、JSON 损坏）
2. 检查 `AppSettings` 结构体字段是否与合同定义一致

### 设置页 UI
1. 检查 `SettingsTab.tsx` 组件结构，确认所有配置项的控件类型
2. 验证自动保存逻辑（修改 → save_settings 调用 → toast 反馈）
3. 验证目录选择按钮交互

### 壁纸自动轮换
1. 重点检查 Rust 定时器实现 — tokio task 创建/取消、Mutex 保护
2. 随机/顺序选择逻辑正确性
3. 空目录处理（不中断定时器）
4. `RotationStatus` 返回值正确性

### 前端集成
1. App.tsx 启动时加载配置的逻辑
2. 配置 state 向子组件的传递
3. 轮换状态轮询（5 秒间隔）

### 遗留问题修复
1. 逐条检查 4 个修复点的代码变更，对照 evaluation.md 中的问题描述

### 构建验证
1. 确认 `npm run build` 和 `cargo build --release` 通过
